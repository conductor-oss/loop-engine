{
  "name": "verify_refund",
  "description": "Independent verification of a refund decision against the ledger + policy (window, max refundable). Evidence-based evaluator, not self-report.",
  "retryCount": 2,
  "retryLogic": "FIXED",
  "retryDelaySeconds": 2,
  "timeoutSeconds": 30,
  "timeoutPolicy": "RETRY",
  "responseTimeoutSeconds": 20,
  "pollTimeoutSeconds": 3600,
  "ownerEmail": "viren@orkes.io",
  "inputKeys": ["order_id", "action", "amount"],
  "outputKeys": ["passed", "score", "feedback", "recommend"]
}
